import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GRECS Refrigeration & Engineering Pvt Ltd - Engineering Towards Excellence",
  description: "GRECS Refrigeration & Engineering Pvt Ltd offers innovative thermal, refrigeration, heating, ventilation, and air conditioning solutions for various industries including food, dairy, pharma, and marine.",
  icons: {
    icon: "/favicon.ico", // Points to the favicon in the public directory
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}